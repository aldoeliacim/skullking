version: "3.8"

services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - ENVIRONMENT=development
    volumes:
      - ./models:/app/models:ro  # Mount trained RL models
